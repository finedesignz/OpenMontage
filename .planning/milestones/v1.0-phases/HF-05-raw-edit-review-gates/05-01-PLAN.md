---
phase: 5
plan: "05-01"
wave: 1
requirements: [EDITGATE-01, EDITGATE-02]
autonomous: true
depends_on: []
files_modified:
  - pipeline_defs/talking-head.yaml
  - pipeline_defs/podcast-repurpose.yaml
  - skills/meta/checkpoint-protocol.md
  - tests/  (a manifest-load + gate assertion test)
---

# Phase 5 / Plan 05-01 — Raw-edit named review gates + checkpoint enforcement

Manifest + skill phase. NO schema change — the stage schema already allows `sub_stages`
with `{name, description, condition, human_approval_default, tools_available, review_focus}`.
Read `schemas/pipelines/pipeline_manifest.schema.json` (stage def) and `lib/pipeline_loader.py`
FIRST to confirm the sub_stages shape and how manifests validate.

## Task 1 (tracer) — name the gates on talking-head edit, manifest still validates

1. In `pipeline_defs/talking-head.yaml`, on the `edit` stage, add `sub_stages` (D-01):
   - `trim_filler` — description = auto jump-cuts / filler removal; `tools_available:
     [silence_cutter, video_trimmer]`.
   - `approve_cut_list` — `human_approval_default: true`; review_focus = the cut list
     (edit_decisions) is correct/complete before any render spend.
   - `animate` — description = proceed to compose/animate only after cut-list approval.
   Flip the `edit` stage-level `human_approval_default` to `true` (D-02).
2. Add/extend a test that loads `talking-head` via `lib/pipeline_loader.py` (validates against
   the manifest schema) and asserts the three named sub_stages exist and
   `approve_cut_list.human_approval_default is True`. Tracer DoD: manifest load-validates
   GREEN + assertions pass. Commit atomically.

## Task 2 (expansion) — podcast-repurpose + checkpoint-protocol + full manifest sweep

1. Apply the same `sub_stages` + `human_approval_default: true` to the `edit` stage of
   `pipeline_defs/podcast-repurpose.yaml`. (Also check screen-demo's edit stage — wire only
   if it has a raw-trim step; otherwise note it as intentionally scoped out.)
2. Add a section to `skills/meta/checkpoint-protocol.md` (D-03): the raw-edit gate sequence
   (trim_filler -> approve_cut_list -> animate), `approve_cut_list` is a MANDATORY
   human-approval gate before compose/animate spend, and how the protocol reads a
   sub-stage's `human_approval_default`. Keep consistent with the existing protocol table +
   the Phase-3 beat-timeline gate section. ASCII-only.
3. **Verify:** load-validate EVERY manifest in `pipeline_defs/` via `lib/pipeline_loader.py`
   (none broken); talking-head + podcast-repurpose edit stages carry the three named
   sub_stages with the human gate on approve_cut_list; checkpoint-protocol documents it;
   ASCII-clean. Commit atomically.

## Definition of Done
- talking-head + podcast-repurpose `edit` stage expresses the named trim_filler ->
  approve_cut_list -> animate gates; approve_cut_list requires human approval.
- checkpoint-protocol.md documents the raw-edit cut-list gate.
- ALL pipeline_defs/*.yaml still load-validate; new test GREEN; ASCII-clean.

<threat_model>
Manifest config + docs. Risk = (a) a malformed sub_stages block breaking manifest load
(mitigated by the load-validate-every-manifest gate), (b) forgetting the human gate so the
cut list finalizes unattended (mitigated by the explicit
`approve_cut_list.human_approval_default is True` assertion). No runtime/security surface.
</threat_model>
