---
phase: HF-01-local-tts-kokoro
plan: 2
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - tools/audio/tts_selector.py
  - tests/tools/test_tts_selector_downgrade.py
  - PROJECT_CONTEXT.md
autonomous: true
requirements: [TTS-03]

must_haves:
  truths:
    - "With ELEVENLABS_API_KEY unset, TTSSelector.execute(preferred_provider='elevenlabs') returns success=False with a 'requested provider unavailable -- not downgrading' message and writes NO WAV (D-05, TTS-03)"
    - "An unknown preferred_provider name still falls through to the auto ranking and picks an available free provider (D-05)"
    - "On a free-only host, operation='rank' orders kokoro above piper via the quality_score lever, no scoring-engine edit (D-05)"
    - "The ElevenLabs -> Kokoro -> Piper precedence + no-downgrade rule is documented in the TTSSelector docstring AND PROJECT_CONTEXT.md's TTS bullet (D-06)"
  artifacts:
    - tools/audio/tts_selector.py
    - tests/tools/test_tts_selector_downgrade.py
    - PROJECT_CONTEXT.md
  key_links:
    - "_select_best_tool: when preferred != 'auto' and unmatched, a KNOWN-but-unavailable provider must return (None, None) instead of falling through to the auto loop (tts_selector.py L178-187)"
    - "execute() must turn that (None, None) into a specific unavailable message, not the generic 'No TTS provider available.' (tts_selector.py L140-141)"
---

<objective>
Expansion (TTS-03): fix the real silent-downgrade bug in the selector and document the fallback
precedence, building on the proven tracer (plan 01-01) where kokoro_tts already carries
quality_score=0.7.

Two things: (a) close the no-silent-downgrade gap in TTSSelector._select_best_tool so an
explicitly-requested-but-unavailable provider (a requested paid voice) surfaces unavailability
instead of quietly downgrading to a free provider (D-05); (b) rely on the already-set quality_score
lever to order Kokoro > Piper on the free path (no scoring-engine edit) and document the
ElevenLabs -> Kokoro -> Piper precedence + no-downgrade contract in the selector docstring and
PROJECT_CONTEXT.md (D-06).

Purpose: make the fallback contract explicit and enforced so pipelines rely on a stated precedence,
not implicit behavior, and a run that asked for a specific voice is never silently substituted.
Output: patched tts_selector.py; a regression test; updated PROJECT_CONTEXT.md TTS bullet.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/HF-01-local-tts-kokoro/01-CONTEXT.md
@.planning/phases/HF-01-local-tts-kokoro/01-RESEARCH.md
@tools/audio/tts_selector.py
@PROJECT_CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix no-silent-downgrade in _select_best_tool + specific unavailable message</name>
  <files>tools/audio/tts_selector.py, tests/tools/test_tts_selector_downgrade.py</files>
  <read_first>01-RESEARCH.md section "D-05 / D-06 -- Selector precedence" including the exact fix snippet and the reworded execute() error message; tts_selector.py L157-187 (_select_best_tool) and L120-141 (execute). Note the A1 assumption.</read_first>
  <behavior>
    - Test 1 (no silent downgrade): given a fake free provider reporting AVAILABLE and a fake "elevenlabs" provider reporting UNAVAILABLE, execute({preferred_provider:'elevenlabs', ...}) returns success=False; error names the requested provider and states it is not downgrading; result writes no WAV and sets no selected_provider.
    - Test 2 (unknown name still auto-ranks): preferred_provider='bogus' (not any registered provider) falls through to the auto ranking and returns the top available free provider (success=True).
    - Test 3 (explicit available honored): preferred_provider set to an available free provider returns exactly that provider.
    Build these hermetically by stubbing the candidate providers (monkeypatch _providers or inject fakes with controlled get_status), so the test needs no ElevenLabs key and no 337 MB model files.
  </behavior>
  <action>
    A1 verify-then-proceed FIRST: trace the accent-safe-voice call path. Grep callers of tts_selector / the tts capability for a request that passes a paid-shaped voice_id WITHOUT setting preferred_provider. If such a caller exists, the "requested paid voice" signal is voice_id-shaped and the fix must treat it as an explicit ElevenLabs request; if no caller relies on voice_id-only, scope the fix to an explicit preferred_provider. Record the finding in a short comment citing A1 and in the summary.
    Then edit _select_best_tool (tts_selector.py L178-187) per RESEARCH: keep the existing "return the preferred provider if it is available" loop; after it, when preferred != 'auto' and no available match was found, compute the set of KNOWN provider names from candidates and, if preferred is in that set, return (None, None) so execute() can surface unavailability — do NOT fall through to the auto loop (that is the D-05 violation). Only an UNKNOWN preferred name falls through to the auto ranking. Then update execute() (L140-141) so a (None, None) that came from an explicit-but-unavailable preference returns success=False with a specific message naming the provider and stating it is not downgrading to a free voice and offering preferred_provider='auto' — distinct from the generic "No TTS provider available." used when there are genuinely no providers. Do not collapse the two selection loops back into one fall-through (RESEARCH pitfall 6). Cite D-05.
  </action>
  <verify>
    <automated>python -m pytest tests/tools/test_tts_selector_downgrade.py -x -q</automated>
  </verify>
  <done>An explicit unavailable paid request returns success=False with the specific not-downgrading message and no WAV; an unknown provider name still auto-ranks to an available free provider; an available explicit preference is honored; regression test guards all three.</done>
  <reversibility rating="costly">Touches selector selection logic and a documented contract; guarded by the regression test so a future "simplify" that reintroduces the fall-through fails CI.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Document ElevenLabs -> Kokoro -> Piper precedence + verify the free-path ordering</name>
  <files>tools/audio/tts_selector.py, PROJECT_CONTEXT.md</files>
  <read_first>01-RESEARCH.md "D-05 / D-06" scoring rationale (why quality_score=0.7 gives Kokoro a deterministic +0.06 edge over Piper on the free path); PROJECT_CONTEXT.md around the capability-first TTS example line (the tts_selector + provider list bullet).</read_first>
  <action>
    Extend the TTSSelector module docstring (tts_selector.py L1-6) to state the fallback precedence — ElevenLabs (when its key is set and a specific voice/clone is requested) -> Kokoro (free, offline, multilingual, higher quality) -> Piper (free, offline, last resort) — and the no-silent-downgrade rule for an explicitly requested provider (D-06). Extend the TTS bullet in PROJECT_CONTEXT.md (the capability-first example listing tts_selector + provider tools) to record the same stated precedence and the "narration with zero paid/dead-key dependency" contract, so future pipelines and agents rely on the documented contract rather than implicit behavior (D-06). Use ASCII punctuation only (hyphens, straight quotes) — the registry scrubs Unicode dashes at runtime and outbound docs must not mojibake. Do not edit lib/scoring.py: the ordering comes from the quality_score lever already set on kokoro_tts (D-05).
  </action>
  <verify>
    <automated>python -c "import os; os.environ.pop('ELEVENLABS_API_KEY', None); from tools.audio.tts_selector import TTSSelector; r=TTSSelector().execute({'text':'rank test','operation':'rank'}); rk=[x['provider'] for x in r.data['rankings']]; assert 'kokoro' in rk, rk; assert 'piper' not in rk or rk.index('kokoro') < rk.index('piper'), rk; print('kokoro>=piper ok', rk)" && python -c "import io; d=open('tools/audio/tts_selector.py',encoding='utf-8').read(); assert 'Kokoro' in d and 'Piper' in d, 'precedence not documented in selector docstring'; p=open('PROJECT_CONTEXT.md',encoding='utf-8').read(); assert 'Kokoro' in p, 'PROJECT_CONTEXT TTS bullet not updated'; print('docs ok')"</automated>
  </verify>
  <done>The selector docstring and PROJECT_CONTEXT.md TTS bullet both state the ElevenLabs -> Kokoro -> Piper precedence and no-downgrade rule; operation='rank' on a free-only host orders kokoro ahead of piper.</done>
  <reversibility rating="reversible">Docstring + doc prose; no behavioral change beyond wording.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller preference -> provider selection | preferred_provider / voice_id chooses which provider (and cost tier) runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-HF01-04 | Spoofing/Elevation | _select_best_tool provider substitution | high | mitigate | The fix itself: an explicit-but-unavailable requested provider returns (None, None) and a specific message, so a run never silently receives a different (free) provider than it asked for. Regression test enforces it. |
| T-HF01-05 | Repudiation | selection provenance | low | accept | execute() already surfaces selected_provider + selection_reason in ToolResult.data so which provider ran is auditable. |
| T-HF01-07 | Tampering | doc mojibake in outbound text | low | mitigate | ASCII-only punctuation in docstring + PROJECT_CONTEXT edits. |
</threat_model>

<verification>
- `python -m pytest tests/tools/test_tts_selector_downgrade.py -q` passes (no-downgrade, unknown-name-auto-ranks, explicit-available-honored).
- No-downgrade DoD (RESEARCH): with ELEVENLABS_API_KEY unset, `TTSSelector.execute(preferred_provider='elevenlabs')` returns success=False with the "unavailable -- not downgrading" message and no Kokoro/Piper WAV.
- Free-path ordering: `operation='rank'` with no ElevenLabs key ranks kokoro ahead of piper.
- Precedence text present in tts_selector docstring and PROJECT_CONTEXT.md.
</verification>

<success_criteria>
- When ElevenLabs is unavailable the selector falls back to Kokoro/Piper with no paid/dead-key call (Success Criterion 3).
- A run that requested a specific ElevenLabs voice does NOT silently degrade; the documented precedence surfaces the substitution (Success Criterion 4).
</success_criteria>

<output>
Create `.planning/phases/HF-01-local-tts-kokoro/01-02-SUMMARY.md` when done. Record the A1
call-site finding (whether any caller signals a paid voice via voice_id-only) and how the fix
handled it.
</output>
