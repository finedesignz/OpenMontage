---
phase: HF-01-local-tts-kokoro
plan: 3
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - tools/audio/kokoro_tts.py
  - tests/tools/test_kokoro_tts.py
  - .agents/skills/kokoro/SKILL.md
autonomous: true
requirements: [TTS-01, TTS-02]

must_haves:
  truths:
    - "A non-English Kokoro voice (e.g. a bf_ British or ff_ French voice) synthesizes a non-silent WAV offline with the espeak lang auto-derived from the voice prefix when no explicit lang is passed (TTS-01, D-02)"
    - "The voice inventory covers 54 voices across 8 languages, verified against the installed kokoro-onnx voices (TTS-01)"
    - "A generic voice_id (no 'voice' key) still lands on the intended Kokoro voice (A3)"
    - ".agents/skills/kokoro/SKILL.md exists as the accurate kokoro-onnx engine reference and kokoro_tts.agent_skills includes 'kokoro' (TTS-02, D-04)"
  artifacts:
    - .agents/skills/kokoro/SKILL.md
    - tools/audio/kokoro_tts.py
    - tests/tools/test_kokoro_tts.py
  key_links:
    - "execute() derives espeak lang from the voice prefix per the RESEARCH map only when no explicit lang is supplied; an explicit lang always wins"
    - "kokoro_tts.agent_skills=['kokoro','text-to-speech'] (set in the tracer) bridges Layer 1 -> Layer 3; the 'kokoro' skill file must actually exist to complete the bridge"
---

<objective>
Expansion (TTS-01 breadth + TTS-02 bridge): grow the proven English tracer (plan 01-01) into the
full 54-voice / 8-language Kokoro provider and author the engine skill that its agent_skills field
references.

Two things: (a) auto-derive the espeak lang from the voice prefix so multilingual voices work
without the caller knowing espeak strings, and confirm the 54/8 inventory + voice_id pass-through;
(b) author a minimal, accurate .agents/skills/kokoro/SKILL.md engine reference (the existing
text-to-speech skill teaches HeyGen, not Kokoro), completing the honest Layer-1 -> Layer-3 bridge
that kokoro_tts.agent_skills=["kokoro","text-to-speech"] declares.

Purpose: deliver the full multilingual capability the roadmap promises and make the agent_skills
bridge real, not a dangling reference.
Output: multilingual execute() + tests; .agents/skills/kokoro/SKILL.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/HF-01-local-tts-kokoro/01-CONTEXT.md
@.planning/phases/HF-01-local-tts-kokoro/01-RESEARCH.md
@tools/audio/kokoro_tts.py
@.agents/skills/doubao-tts/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Multilingual voice-prefix -> espeak lang derivation + inventory / voice_id verify</name>
  <files>tools/audio/kokoro_tts.py, tests/tools/test_kokoro_tts.py</files>
  <read_first>01-RESEARCH.md "Model / voice facts" (the 9-prefix -> 8-language map and espeak lang strings) and the Assumptions Log entries A2 and A3.</read_first>
  <behavior>
    - Test 1 (multilingual auto-lang): execute({"text": ..., "voice": "<a non-en voice, e.g. bf_ or ff_>", "output_path": <tmp>.wav}) with NO explicit lang derives the correct espeak lang from the prefix and writes a non-silent 24000 Hz WAV.
    - Test 2 (explicit lang wins): passing an explicit lang overrides the prefix-derived value.
    - Test 3 (voice_id pass-through): execute({"text": ..., "voice_id": "af_heart"}) with no "voice" key still synthesizes on af_heart (A3).
    - Test 4 (inventory): the installed voices expose 54 voices across the 8 documented languages.
    Guard synthesis tests with a skip when model files are absent so the suite stays green off-host.
  </behavior>
  <action>
    A2 verify-then-proceed FIRST: enumerate the installed Kokoro voices (kokoro-onnx get_voices() / the bundled VOICES listing) and confirm the prefix set and count match the RESEARCH map (54 voices, 8 languages, prefixes af/am/bf/bm/ef/em/ff/hf/hm/if/im/jf/jm/pf/pm/zf/zm). Confirm the espeak lang strings per prefix; proceed with the RESEARCH table if it matches, correct any drift, and record it in the summary.
    Then extend execute() so that when inputs has no explicit "lang", it derives the espeak lang from the selected voice's prefix using the verified map (falling back to en-us for an unknown prefix); an explicit inputs["lang"] always overrides. Keep the tracer's voice/voice_id/af_heart resolution and the singleton Kokoro load unchanged. Cite D-02 (multilingual is the field that differs from Piper).
  </action>
  <verify>
    <automated>python -m pytest tests/tools/test_kokoro_tts.py -x -q</automated>
  </verify>
  <done>A non-English voice synthesizes offline with auto-derived lang; explicit lang overrides; voice_id pass-through lands; the 54/8 inventory is confirmed against the installed voices.</done>
  <reversibility rating="reversible">Additive lang-derivation branch in execute() plus tests; the English happy path is unchanged.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Author .agents/skills/kokoro/SKILL.md engine reference (Layer-3 bridge)</name>
  <files>.agents/skills/kokoro/SKILL.md</files>
  <read_first>01-RESEARCH.md "agent_skills bridge (TTS-02)" section (why the existing text-to-speech skill is HeyGen-specific and a dedicated kokoro skill is needed); .agents/skills/doubao-tts/SKILL.md for the minimal local-engine frontmatter + prose shape to mirror.</read_first>
  <action>
    Author a minimal (~1 page) .agents/skills/kokoro/SKILL.md with YAML frontmatter name kokoro and a description of offline free Kokoro-82M narration. Body documents: pip install kokoro-onnx==0.5.0 + soundfile; the one-time download of kokoro-v1.0.onnx and voices-v1.0.bin into ~/.kokoro (override KOKORO_MODEL_DIR); that espeak-ng must NOT be hand-installed (espeakng-loader bundles it); the engine API Kokoro(onnx_path, voices_path).create(text, voice, speed, lang) returning (samples, sample_rate=24000); soundfile.write to persist; the 54-voice / 8-language inventory with the voice-prefix -> espeak-lang map; the default voice af_heart; and the Apache-2.0 license (free for commercial use). This is the accurate engine reference kokoro_tts.agent_skills=["kokoro"] bridges to (TTS-02, D-04). ASCII punctuation only.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; p=Path('.agents/skills/kokoro/SKILL.md'); t=p.read_text(encoding='utf-8'); assert t.lstrip().startswith('---') and 'name: kokoro' in t, 'missing frontmatter/name'; assert all(s in t for s in ['kokoro-onnx','create(','24000','af_heart','Apache-2.0']), 'engine facts missing'; print('skill ok')" && python -c "from tools.audio.kokoro_tts import KokoroTTS; assert 'kokoro' in KokoroTTS.agent_skills, KokoroTTS.agent_skills; print('bridge ok')"</automated>
  </verify>
  <done>.agents/skills/kokoro/SKILL.md exists with name: kokoro frontmatter and documents the kokoro-onnx engine (install, model files, create() API, 24000 Hz, voice map, af_heart, Apache-2.0); kokoro_tts.agent_skills includes 'kokoro' so the Layer-1 -> Layer-3 bridge is complete.</done>
  <reversibility rating="reversible">New standalone skill doc; no code path depends on its contents beyond the name reference.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller output_path -> filesystem | multilingual execute() still writes a WAV to a caller-supplied path |
| skill doc -> agent behavior | SKILL.md instructs agents to pip install + download model files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-HF01-06 | Tampering | execute() output_path write | low | accept | Same local-file pattern as piper_tts (existing accepted behavior); local offline tool, no privilege boundary crossed. |
| T-HF01-08 | Tampering | SKILL.md install/download guidance | low | mitigate | Skill pins kokoro-onnx==0.5.0 and the versioned model-files-v1.0 URLs (matches the audited legitimacy entries); no floating/latest, no third-party mirror. |
</threat_model>

<verification>
- `python -m pytest tests/tools/test_kokoro_tts.py -q` passes (multilingual auto-lang, explicit-lang-override, voice_id pass-through, 54/8 inventory).
- `.agents/skills/kokoro/SKILL.md` exists with `name: kokoro` frontmatter and documents kokoro-onnx, create(), 24000, af_heart, Apache-2.0.
- `KokoroTTS.agent_skills` includes 'kokoro'.
</verification>

<success_criteria>
- kokoro_tts delivers the full 54-voice / 8-language offline synthesis promised by TTS-01.
- The agent_skills[] bridge to a .agents/skills engine reference is complete and accurate (Success Criterion 2, bridge half; TTS-02).
</success_criteria>

<output>
Create `.planning/phases/HF-01-local-tts-kokoro/01-03-SUMMARY.md` when done. Record the A2 inventory
finding (whether the installed voice prefixes + espeak lang strings matched RESEARCH).
</output>
