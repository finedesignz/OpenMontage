---
phase: HF-01-local-tts-kokoro
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - tools/audio/kokoro_model_setup.py
  - tools/audio/kokoro_tts.py
  - tests/tools/test_kokoro_tts.py
autonomous: true
requirements: [TTS-01, TTS-02, TTS-03]
user_setup: []

must_haves:
  truths:
    - "Running kokoro_tts.execute({text, output_path}) with no paid key and no network at inference produces a valid 24000 Hz non-silent WAV (TTS-01)"
    - "registry.get_by_capability('tts') lists a provider named kokoro_tts via auto-discovery — no selector edit (D-01)"
    - "TTSSelector.execute(preferred_provider='kokoro') with ELEVENLABS_API_KEY unset selects kokoro and writes a WAV offline"
    - "kokoro_tts.quality_score == 0.7 is set so the free-path scoring lever orders Kokoro over Piper (D-05, TTS-03)"
  artifacts:
    - tools/audio/kokoro_tts.py
    - tools/audio/kokoro_model_setup.py
    - tests/tools/test_kokoro_tts.py
    - requirements.txt
  key_links:
    - "kokoro_tts.get_status() gates on BOTH import kokoro_onnx AND both model files present in KOKORO_MODEL_DIR — else the selector routes to Kokoro then fails at execute (RESEARCH pitfall 4)"
    - "dependencies=['python:kokoro_onnx'] matches base_tool.check_dependencies python: prefix (base_tool.py L224)"
    - "execute() reads inputs.get('voice') or inputs.get('voice_id') or 'af_heart' so the selector's generic voice_id pass-through lands (A3)"
---

<objective>
Tracer slice for Phase 1: prove free, offline Kokoro-82M synthesis end-to-end through the
OpenMontage BaseTool + tts_selector contract, on the agent's best early-context tokens, before
any expansion (selector bug-fix, multilingual breadth, skill authoring, docs).

Deliver a production-quality `kokoro_tts` provider (D-01, mirrors piper_tts.py) that a real caller
can invoke — directly and via the auto-discovering registry — to turn text into a non-silent WAV
with zero paid/dead-key dependency. This is the thinnest path that touches every layer the phase
modifies: dependency install + one-time model download, the provider tool, and registry
auto-discovery. Multilingual/voice breadth and the selector precedence fix expand out from this
proven slice in wave 2 (plans 01-02, 01-03).

Purpose: unblock free narration for every pipeline; establish that the offline CPU path works on
this Windows host before investing in breadth.
Output: kokoro-onnx pinned in requirements; model files cached in ~/.kokoro; kokoro_tts.py;
tests/tools/test_kokoro_tts.py.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/HF-01-local-tts-kokoro/01-CONTEXT.md
@.planning/phases/HF-01-local-tts-kokoro/01-RESEARCH.md
@tools/audio/piper_tts.py
@tools/base_tool.py
@tools/tool_registry.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin kokoro-onnx deps and download model files (one-time, offline thereafter)</name>
  <files>requirements.txt, tools/audio/kokoro_model_setup.py</files>
  <precondition>Network reachable for a single ~337 MB download from github.com (release tag model-files-v1.0). After this task, inference is fully offline; the helper is idempotent and skips files already present.</precondition>
  <read_first>01-RESEARCH.md sections "Standard Stack" (pinned versions + model URLs), "Package Legitimacy Audit" (all Approved/[VERIFIED]), and "Common Pitfalls" item 1 (do NOT hand-install system espeak-ng).</read_first>
  <action>
    Add two pinned dependencies to requirements.txt under a "Local TTS (Kokoro)" comment: kokoro-onnx==0.5.0 and soundfile>=0.12 (per D-03, TTS-02; versions from RESEARCH Standard Stack — do not re-derive). Create tools/audio/kokoro_model_setup.py exposing an idempotent ensure_models(model_dir=None) helper plus a __main__ entry: resolve model_dir from KOKORO_MODEL_DIR env or default to Path.home()/'.kokoro', create it, and download the two versioned release assets kokoro-v1.0.onnx and voices-v1.0.bin from the model-files-v1.0 release tag over HTTPS, skipping any file already on disk. Pin the exact release-tag URLs given in RESEARCH (do not follow a floating "latest"). Do NOT install or touch system espeak-ng — espeakng-loader ships the shared lib transitively (RESEARCH pitfall 1); a conflicting system install shadows it. Then pip install the two deps and run the helper once so the model files land in ~/.kokoro.
  </action>
  <verify>
    <automated>python -c "import kokoro_onnx, soundfile; print('deps ok')" && python -c "import os; from pathlib import Path; d=Path(os.environ.get('KOKORO_MODEL_DIR', Path.home()/'.kokoro')); assert (d/'kokoro-v1.0.onnx').is_file() and (d/'voices-v1.0.bin').is_file(), f'model files missing under {d}'; print('models ok')"</automated>
  </verify>
  <done>requirements.txt pins kokoro-onnx==0.5.0 + soundfile>=0.12; both model files exist under KOKORO_MODEL_DIR; import kokoro_onnx and import soundfile succeed.</done>
  <reversibility rating="reversible">Isolated helper + two additive requirements lines; removable without touching existing providers.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tracer — kokoro_tts BaseTool provider + offline synthesis smoke (English happy path)</name>
  <files>tools/audio/kokoro_tts.py, tests/tools/test_kokoro_tts.py</files>
  <read_first>01-RESEARCH.md "Exact kokoro_tts.py BaseTool field values" table (every field + which differ from Piper), the get_status() snippet (import AND model-files gate), the "Minimal engine call" snippet, and the input_schema block. Mirror the structure of tools/audio/piper_tts.py.</read_first>
  <behavior>
    - Test 1 (offline synthesis): execute({"text": "Offline Kokoro test.", "output_path": <tmp>.wav}) with ELEVENLABS_API_KEY unset returns ToolResult.success is True; the WAV exists; soundfile reads sample_rate == 24000 and max(abs(samples)) > 0.01 (non-silent).
    - Test 2 (auto-discovery): registry.ensure_discovered(); "kokoro_tts" is in [t.name for t in registry.get_by_capability("tts")] (D-01 — no selector edit).
    - Test 3 (through the contract): TTSSelector().execute({"text": ..., "preferred_provider": "kokoro", "output_path": <tmp>.wav}) returns success with data["selected_provider"] == "kokoro" and the file exists, ELEVENLABS_API_KEY unset.
    - Test 4 (get_status gating): with KOKORO_MODEL_DIR pointed at an empty dir, get_status() returns UNAVAILABLE even though kokoro_onnx imports (RESEARCH pitfall 4). Guard the synthesis tests with a skip when models are absent so the suite stays green on hosts without them.
  </behavior>
  <action>
    Implement class KokoroTTS(BaseTool) mirroring piper_tts.py, using the RESEARCH field table verbatim: name kokoro_tts, provider kokoro (D-01), tier VOICE, capability tts, stability EXPERIMENTAL, execution_mode SYNC, determinism DETERMINISTIC, runtime ToolRuntime.LOCAL (D-01); dependencies ["python:kokoro_onnx"] (D-03 — the python: prefix base_tool supports, not a cmd: binary); install_instructions = the pip line + the two model-file download URLs; agent_skills = ["kokoro", "text-to-speech"] (D-04, TTS-02 — the "kokoro" engine skill is authored in plan 01-03); capabilities include text_to_speech, offline_generation, multilingual_generation; supports = {voice_cloning: False, multilingual: True, offline: True, native_audio: True} (D-02); best_for / not_good_for per the table (D-02); input_schema mirroring Piper's shape with text/voice(default af_heart)/speed(default 1.0)/lang(default en-us)/output_path (D-04); resource_profile, retry_policy, idempotency_key_fields, side_effects, user_visible_verification per the table; quality_score = 0.7 as a class attribute (D-05 — the free-path precedence lever consumed by lib/scoring.py, no scoring edit); estimate_cost returns 0.0 (TTS-02 cost=0). Override get_status() to return AVAILABLE only when import kokoro_onnx succeeds AND both model files are present under KOKORO_MODEL_DIR (env override, default ~/.kokoro) — else UNAVAILABLE. In execute(): read the voice as inputs.get("voice") or inputs.get("voice_id") or "af_heart" so the selector's generic voice_id passes through (A3); read speed (default 1.0) and lang from inputs.get("lang", "en-us") — this tracer keeps lang caller-supplied/default only, auto-derivation from the voice prefix is plan 01-03; load a module-level singleton Kokoro(onnx_path, voices_path) keyed by model dir and reuse it (RESEARCH pitfall 3 — do not re-instantiate per call); call kokoro.create(text, voice, speed, lang) to get (samples, sr) and write with soundfile.write(output_path, samples, sr) (RESEARCH pitfall 2 — create() returns a float32 array, not a file); return a ToolResult whose data payload mirrors piper_tts (provider, voice, text_length, output, format "wav") with artifacts=[output_path]. No subprocess, no network.
    A3 verify-then-proceed: before finalizing, grep the repo for callers that invoke a TTS tool with Piper's "model"/"speaker_id" keys but no "voice"/"voice_id"; confirm the voice/voice_id fallback covers every call site, and record the finding in the summary. If a caller relies on model/speaker_id only, add that key to the fallback chain rather than assuming.
  </action>
  <verify>
    <automated>python -m pytest tests/tools/test_kokoro_tts.py -x -q && python -c "from tools.tool_registry import registry; registry.ensure_discovered(); assert 'kokoro_tts' in [t.name for t in registry.get_by_capability('tts')], 'kokoro_tts not discovered'; print('discovery ok')"</automated>
  </verify>
  <done>kokoro_tts is auto-discovered as a capability=tts provider; a direct execute() and a TTSSelector(preferred_provider='kokoro') call each write a 24000 Hz non-silent WAV offline with ELEVENLABS_API_KEY unset; get_status() gates on import AND model files. The single happy path works end-to-end and is committed.</done>
  <reversibility rating="reversible">New isolated provider file (D-01); no existing provider or selector touched in this plan.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub release -> local disk | One-time model-file download crosses into ~/.kokoro (untrusted-origin binary) |
| PyPI -> local env | pip installs kokoro-onnx + soundfile + transitive deps |
| caller text/output_path -> local filesystem | execute() writes a WAV to a caller-supplied path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-HF01-01 | Tampering | kokoro_model_setup.py download | medium | mitigate | Pin the exact versioned release-tag URLs (model-files-v1.0), HTTPS only, idempotent skip-if-present; no floating "latest". |
| T-HF01-SC | Tampering | pip installs (kokoro-onnx, soundfile) | high | mitigate | Pinned versions; RESEARCH Package Legitimacy Audit rates both Approved/[VERIFIED] with no [ASSUMED]/[SUS]/[SLOP] entries, so no blocking human checkpoint is required by the package-legitimacy gate. |
| T-HF01-02 | Denial of Service | execute() unbounded text | low | accept | retry_policy max_retries=1; text size is caller-controlled; local CPU only. |
| T-HF01-03 | Information disclosure | inference network egress | low | mitigate | Design is offline: network_required=False, no API call in the synthesis path; only the one-time model download touches the network. |
</threat_model>

<verification>
- Deps import: `python -c "import kokoro_onnx, soundfile"` succeeds.
- Model files present under KOKORO_MODEL_DIR (default ~/.kokoro).
- `python -m pytest tests/tools/test_kokoro_tts.py -q` passes (offline synthesis, discovery, contract, get_status gating).
- DoD direct-engine offline proof (RESEARCH Verification): run the one-line `from kokoro_onnx import Kokoro ...` synth to kokoro_test.wav and confirm `OK 24000 <n>`; for a hard offline proof, run it with the network disabled.
- Through-the-contract: `registry.get_by_capability('tts')` includes kokoro_tts; `TTSSelector.execute(preferred_provider='kokoro', ELEVENLABS_API_KEY unset)` returns success with selected_provider == 'kokoro' and the file exists.
</verification>

<success_criteria>
- kokoro_tts synthesizes a WAV from text offline, no paid/dead key (Success Criterion 1).
- registry lists kokoro_tts as a capability=tts provider via auto-discovery (Success Criterion 2, discovery half).
- quality_score=0.7 is set (the TTS-03 free-path ordering lever; ordering verified in plan 01-02).
</success_criteria>

<output>
Create `.planning/phases/HF-01-local-tts-kokoro/01-01-SUMMARY.md` when done. Record the A3 call-site
finding (whether any caller relies on model/speaker_id-only) for plans 01-02 and 01-03.
</output>
