# OpenMontage - Core Dependencies
pyyaml>=6.0
pydantic>=2.0
jsonschema>=4.20
python-dotenv>=1.0
Pillow>=10.0
requests>=2.31
google-auth>=2.0       # service-account auth for Google TTS + Imagen (Vertex AI)

# Local TTS (Kokoro) - free, offline neural TTS (Kokoro-82M via ONNX Runtime)
kokoro-onnx==0.5.0     # ONNX inference; bundles espeak-ng via espeakng-loader (no system install)
soundfile>=0.12        # write kokoro.create() float32 samples to WAV (24000 Hz)
